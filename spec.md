# プロジェクト指示書：溶融亜鉛めっき現場向け「技術伝承AIバイブル（品管特化）」試作版

## 1. 目的と運用コンセプト
品管担当者（三浦）と大ベテラン職人（村上本部長）の業務負荷を極限までゼロにする対話型ナレッジ蓄積Webアプリケーションを構築する。

- **三浦さん（質問側）：** 現場の気になる事象をスマホでメモ（または写真添付）して投稿。専門用語の指定やタグ付けなどのメタデータ入力は一切不要。曖昧な殴り書きでも、AIが職人向けに具体的・論理的な質問へ自動リライトする。
- **村上本部長（回答側）：** 画面上の質問ボタンを押すと、スマホがAI音声で質問を読み上げ、自動でマイクが起動。口頭で返答するだけで、Gemini APIが「めっき現場特有の誤変換パターン辞書」を参照しながら自動補正し、構造化QA（現象・原因・処置・予防策）を作成してDBへ自動登録する。
- **初期プリセット：** めっき品管特化の200問インデックスを搭載し、分野別（CAT-1〜CAT-6）にアコーディオン開閉可能。

---

## 2. 技術スタック
- **Framework:** Next.js (App Router, TypeScript)
- **Styling:** Tailwind CSS, lucide-react（アイコン）
- **AI / LLM:** Google Generative AI SDK (`@google/generative-ai`)
  - モデル: `gemini-1.5-flash`（音声マルチモーダル解析・構造化JSON出力・質問リライト）
- **Web APIs:**
  - `window.speechSynthesis`（質問の音声読み上げ）
  - `navigator.mediaDevices.getUserMedia` & `MediaRecorder`（音声録音）
- **Database & Storage:** Supabase (`@supabase/supabase-js`)
  - PostgreSQL DB (`knowledge_records`, `questions_queue`)
  - Storage Bucket (`knowledge-media`: 音声・画像保存用)

---

## 3. 画面設計とUI/UX仕様（2ペイン構成）

### 【左ペイン：200問インデックス ＆ 三浦さんの質問投稿】
1. **三浦さん用「新規質問追加」フォーム（最上部）:**
   - 自由入力テキストエリア（「パイプの先が黒ずんでる」等の殴り書きでOK）
   - 写真アップロード（任意・複数可）
   - 「質問をAIで具体化して登録」ボタン
   - 押下時：`POST /api/refine-question` を呼び出し、Geminiが「見極めポイント＋手直し基準」を含む具体的質問に自動整形して未回答キューへ追加。
2. **インクリメンタル検索バー:** キーワード入力で質問リスト（タイトル・タグ）を即時絞り込み。
3. **分野別アコーディオンリスト（CAT-1〜CAT-6）：**
   - デフォルトは折りたたみ（クリックで開閉）。
   - 各質問カードに「No.」「タイトル」「未回答/回答済みバッジ」を表示。
   - 質問カードをクリックすると、右ペインの「インタビューワークスペース」に対象質問がセットされる。

### 【右ペイン：村上本部長用 音声インタビュー＆バイブル閲覧】
1. **AIインタビューワークスペース（上部）:**
   - 選択された質問タイトル・カテゴリを大きく表示。
   - **「🔊 質問を聞いて回答する」巨大ボタン:**
     1. ブラウザが `speechSynthesis` で「本部長、[質問文] について教えてください」と読み上げ。
     2. 読み上げ終了と同時に `MediaRecorder` が自動起動し、録音状態（赤いマイク波形アニメーション）へ遷移。
     3. 本部長が口頭で回答。
     4. 「話し終わったらタップ（登録）」ボタンを押すと録音停止。
     5. 録音Blobをバックエンド `POST /api/process-knowledge` へ送信。
   - ※PC操作・手動操作用として、既存の「音声・写真のドラッグ＆ドロップ枠」も下部に併設。
2. **品管バイブル（蓄積QA一覧・下部）:**
   - 登録されたQAが即座にカード形式で新着順表示。
   - カード内容：【タイトル】【カテゴリ】【現象】【原因】【処置・合否基準】【再発防止】【生音声プレイヤー】【現場写真】【全文文字起こし（アコーディオン）】。

---

## 4. めっき現場特有の誤変換パターン ＆ 完全辞書定義

システムプロンプト内に定数として埋め込み、Geminiの認識精度を最大化する。

```typescript
export const GALVA_TERMINOLOGY_PROMPT = `
【最重要：音声認識時の誤変換・聞き間違い補正ルール】
音声から以下の「誤変換されやすい音」を検出した場合、溶融亜鉛めっきの文脈に基づき、右側の【正式用語】へ自動的に名寄せ・補正してください。

・「リラックス」「ふらつく」「フラック」 → 【フラックス（助剤・塩化アンモニウム亜鉛）】
・「踏歴」「不明機」「ふめっき」「目利き」 → 【不めっき（地肌露出欠陥）】
・「参戦」「温泉」「感染」「三線」 → 【酸洗（塩酸・硫酸洗浄）】
・「泥巣」「ドレス」「泥ス」 → 【ドロス（鉄亜鉛合金の沈殿・浮遊カス）】
・「人吉」「信金率」「前屈一致」 → 【ジンクリッチ（高濃度亜鉛末塗料）】
・「サンドイッチ現象」「三輪現象」 → 【サンドリン現象（ケイ素起因の超厚膜・灰色化）】
・「白錆」「白波」「白壁」 → 【白サビ（初期の塩基性炭酸亜鉛・酸化亜鉛）】
・「つらら」「つら」「吊ら」 → 【ツララ（引き上げ時に固まった亜鉛の滴）】
・「タレ」「誰」「垂れ」 → 【タレ（過剰亜鉛の流動跡）】
・「ガラ掛け」「からかけ」「柄掛け」 → 【ガラ掛け（ボルト・小物の遠心脱水めっき）】
・「ミルスケ」「見る助」「見過ごす」 → 【ミルスケール（熱間圧延時の黒皮）】
・「スラグ」「フラグ」「プラグ」 → 【溶接スラグ】
・「スパッタ」「バッタ」「パター」 → 【溶接スパッタ】
・「エルエムイー」「割れ」 → 【LME（液体金属脆化割れ）】
・「水銀」「脆化」 → 【水素脆化（酸洗時の水素吸蔵による割れ）】
・「アンカーパターン」「怒り」 → 【アンカーパターン（ブラストによる表面粗さ）】
・「ダブルディップ」「二度漬け」 → 【ダブルディップ（大型材の分割浸漬）】
・「ハイテン」「配点」 → 【高張力鋼（ハイテン材）】
・「リンギ」「臨時」「リンス」 → 【リンギ（製品同士の接触を防ぐ木製スペーサー）】
・「灰」「ハイ」「廃」 → 【酸化亜鉛灰（トップドロス）】

【めっき現場・品質管理 完全専門用語辞書】
■前処理・薬品：
脱脂槽, アルカリ脱脂, 酸洗槽, 塩酸, 硫酸, インヒビター（酸洗抑制剤）, 水洗, フラックス液, 塩化亜鉛, 塩化アンモニウム, 鉄分濃度, ピッティング（孔食）, 戻り錆, エッチング

■めっき浴・浸漬：
溶融亜鉛槽, 亜鉛地金（インゴット）, 浴温（445℃〜450℃）, アルミ添加, ビスマス, ニッケル, ボトムドロス, トップドロス（灰）, 灰払い, 浸漬速度, 引き上げ速度, 液切れ, 揺動, エアブロー, 遠心脱水機

■後処理・冷却：
急冷, 水冷槽, 温水洗浄, 自然放冷（空冷）, クロメート処理, ノンクロメート防錆処理, 水酸化亜鉛, 塩基性炭酸亜鉛

■外観欠陥・物性判定：
不めっき, ヤケ（灰色化）, 光沢ムラ, ストライエーション（波打ち模様）, 灰噛み, ドロス付着, ザラ肌, ブツ, ブリスター（フクレ）, 密着不良, 剥離（ピーリング）, タレ, ツララ, 亜鉛溜まり, 危険バリ, 酸吹き, ピンホール, LME割れ, 水素脆化割れ, 熱歪み, プレス矯正, 線状加熱

■検査・規格・膜厚：
JIS H 8641, 膜厚記号（HDZ35, HDZ45, HDZ55, HDZT）, 電磁式膜厚計, プローブ校正, 5点平均法, 付着量（g/m2）, 硫酸銅試験（均一性試験）, ハンマリング試験（密着性試験）, すべり耐力面（摩擦接合面）, ミルシート（鋼材検査証明書）, 膜厚証明書, トレーサビリティ

■再処理・構造：
空気抜き孔, 亜鉛抜き孔, 閉鎖断面, 水蒸気爆発, ガードレール再めっき, 旧塗膜剥離, 酸剥離, タッチアップ, ジンクリッチペイント（JIS K 5553）
`;